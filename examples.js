const rxl = require('rxl.js')

console.log(rxl.getUserdataFromId(1)); // See? Pretty simple isn't it

console.log(rxl.getUserGroupsFromId(1)); //Get's the user's group from the userid

console.log(rxl.getUserdataFromUsername("AstralNetwork")); //Get's the user's data such as their ID, Username, Presence, etc

console.log(rxl.getIdFromUsername("AstralNetwork")); //Returns the user's ID

console.log(rxl.getUsernameFromId(78421035)); //Returns the user's username from ID

//You can do it, I believe in you\\
